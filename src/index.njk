---
layout: layout.njk
title: Home
---

<link rel="stylesheet" href="/styles/blogStyle.css">

<h1 id="heading">Hello there, I'm <tag>Mark!<tag></h1>
<p>Nice to meet you and welcome to my Digital Garden. You've traveled a lot before you got here so please, take a break.</p>
<br><br><br><br><br>

<h1>Writings</h1>
<div class="posts">
  {% for post in collections.posts %}
    <article>
      <div class="titleContainer">
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        {% if post.data.tags %}
          <p class="post-tags">{{ post.data.tags | join(", ") }}</p>
        {% endif %}
      </div>
     <p id = "intro" >{{ post.templateContent | striptags | truncate(150, true, "...") }}</p>
    </article>
  {% endfor %}
</div>
