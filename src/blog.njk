---
layout: layout.njk
title: Blog
---

<link rel="stylesheet" href="/styles/postsStyle.css">

<h1 id = "heading">Blog</h1>
<p>Welcome to my own space station, don't yu dare go hollow!</p>
<br>
<hr>
<br>

<div class="posts">
  {% for post in collections.posts %}
    <article>
      <div class="titleContainer">
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        {% if post.data.tags %}
          <p class="post-tags">{{ post.data.tags | join(", ") }}</p>
        {% endif %}
      </div>
      <p id ="date"><small>{{ post.date | dateDisplay  }}</small></p>
      <p id = "intro" >{{ post.templateContent | striptags | truncate(150, true, "...") }}</p>
    </article>
  {% endfor %}
</div>
