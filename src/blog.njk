---
layout: layout.njk
title: Blog
---

<h1>Blog</h1>
<p>Benvenuto nel mio spazio di scrittura.</p>
<br>
<hr>
<br>

<div class="posts">
  {% for post in collections.posts %}
    <article>
      <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
      {% if post.data.tags %}
        <p class="post-tags">[{{ post.data.tags | join(", ") }}]</p>
      {% endif %}
      <p><small>{{ post.date | dateDisplay  }}</small></p>
      <p>{{ post.templateContent | striptags | truncate(150, true, "...") }}</p>
    </article>
  {% endfor %}
</div>
